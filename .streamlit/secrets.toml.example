# Copy to secrets.toml (gitignored). Fill in one option only.

# Free tier: https://console.groq.com
GROQ_API_KEY = "gsk_..."

# Or local Ollama — install from https://ollama.com, then: ollama pull llama3.2
# OLLAMA_MODEL = "llama3.2"
